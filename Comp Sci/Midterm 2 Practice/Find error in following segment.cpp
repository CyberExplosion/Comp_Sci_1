#include <iostream>
using namespace std;

/*int main_done4() {
	int* zPtr;
	int number;
	int z[5] = { 1,2,3,4,5 };

	zPtr = z;

	number = zPtr; // zPtr is a pointer that store the address of the first value of array z[], number is an integer data type and cannot be assigned an address to.

	for (int i = 0; i <= 5; i++) { // There are only 5 slots in array z[], ranging from z[0] to z[4]. This loop will read the value of z[5], which does not exist and out of bound of the array
		cout << zPtr[i] << endl;
	}


	return 0;
}

*/