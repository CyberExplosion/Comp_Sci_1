#include <iostream>
#include <fstream>
#include <string>
using namespace std;



int main() {
	fstream file("insert sort.txt", ios::out | ios::in | ios::binary);
	//Record of People
	struct 

	if (file.fail()) {
		cout << "The file failed to open\n";
		return 1;
	}



	return 0;
}